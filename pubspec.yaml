name: erasmus_app
description: A new Flutter project.

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  #To open the browser
  url_launcher: ^4.2.0+3
  #Json
  json_annotation: ^2.0.0
  #TFile paths on IOS and Android
  path_provider: ^0.4.1
  #Permissions
  simple_permissions: ^0.1.9
  #Random uuids
  uuid: ^1.0.3
  #Date formatting
  intl: ^0.15.7
  #Sticky headers
  sticky_headers: ^0.1.7
  #Rich text editor
  zefyr:
    path: ../zefyr/packages/zefyr/
  #Markdown renderer
  flutter_markdown:
    git:
      url: https://github.com/flutter/flutter_markdown.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.1.2
  json_serializable: ^2.0.1


# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
  - 'assets/images/'
  - 'assets/countries/'
  - 'assets/countries/germany/'
  - 'assets/countries/greece/'
  - 'assets/countries/lithuania/'
  - 'assets/countries/slovakia/'
  - 'assets/countries/turkey/'
  - 'assets/schools/'
  - 'assets/schools/hessenwaldschule/'
  - 'assets/schools/gymnasium_chalkida/'
  - 'assets/schools/gamca/'
  - 'assets/schools/juliaus_janonio_gimnazija/'
  - 'assets/schools/muhittin_mustafa_boecek_anadolu_lisesi/'